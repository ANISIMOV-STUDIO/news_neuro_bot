# 🤖 NeuroScov Bot

Автоматизированный AI-ассистент для Telegram-канала "Scov Touch", работающий на базе Gemini AI.

## 📋 Описание

NeuroScov Bot автоматически:
- 📡 Собирает новости из RSS лент и Telegram каналов
- 🧠 Обрабатывает их через Gemini AI в стиле "нейроскуфа"
- 📤 Публикует готовый контент в ваш Telegram канал
- 🕐 Работает по расписанию (2-4 раза в день)
- 💾 Не допускает дубликатов контента

## 🏗️ Архитектура

Проект разделен на независимые модули:

```
news_neuro_bot/
├── src/
│   ├── config_loader.py      # Загрузка конфигурации
│   ├── db_handler.py          # Работа с базой данных
│   ├── source_aggregator.py   # Сбор контента (RSS + Telegram)
│   ├── gemini_processor.py    # Обработка через Gemini AI
│   ├── telegram_poster.py     # Публикация в Telegram
│   ├── scheduler.py           # Управление расписанием
│   └── main.py                # Точка входа
├── data/                      # База данных
├── logs/                      # Логи работы
├── .env                       # Конфигурация (не в git)
├── .env.example               # Пример конфигурации
├── requirements.txt           # Python зависимости
└── README.md                  # Этот файл
```

## 🚀 Быстрый старт

### 1. Требования

- Python 3.10 или выше
- Telegram аккаунт
- API ключи (см. ниже)

### 2. Установка

```bash
# Клонируйте репозиторий
git clone https://github.com/ANISIMOV-STUDIO/news_neuro_bot.git
cd news_neuro_bot

# Создайте виртуальное окружение
python -m venv venv

# Активируйте его
# Linux/Mac:
source venv/bin/activate
# Windows:
venv\Scripts\activate

# Установите зависимости
pip install -r requirements.txt
```

### 3. Настройка

#### 3.1. Получение API ключей

**Gemini API:**
1. Перейдите на https://ai.google.dev/
2. Создайте API ключ
3. Скопируйте его

**Telegram Bot Token:**
1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте полученный токен
5. **Важно:** Добавьте бота в ваш канал как администратора

**Telegram User API (для чтения каналов):**
1. Перейдите на https://my.telegram.org/apps
2. Войдите в свой аккаунт
3. Создайте приложение
4. Скопируйте `api_id` и `api_hash`

#### 3.2. Конфигурация

```bash
# Скопируйте пример конфигурации
cp .env.example .env

# Отредактируйте .env файл
nano .env  # или любой другой редактор
```

Заполните обязательные поля в `.env`:

```env
# Основные настройки
GEMINI_API_KEY=your_gemini_api_key_here
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
TARGET_CHANNEL_ID=@your_channel_name

# Для чтения Telegram каналов
USER_API_ID=your_api_id
USER_API_HASH=your_api_hash

# Источники контента
RSS_FEEDS=https://example.com/rss1.xml,https://example.com/rss2.xml
TELEGRAM_SOURCE_CHANNELS=@tech_news,@it_news
```

### 4. Первый запуск

#### 4.1. Авторизация Telethon (User API)

При первом запуске Telethon попросит вас авторизоваться:

```bash
cd src
python source_aggregator.py
```

Следуйте инструкциям:
1. Введите номер телефона
2. Введите код из Telegram
3. Если включена 2FA - введите пароль

Это нужно сделать только один раз. Сессия сохранится в файле `neuroscov_user.session`.

#### 4.2. Тестовый запуск

```bash
# Тест конфигурации
cd src
python config_loader.py

# Тест базы данных
python db_handler.py

# Тест Gemini API
python gemini_processor.py

# Тест Telegram бота
python telegram_poster.py

# Тест полного цикла (БЕЗ публикации)
python scheduler.py
```

#### 4.3. Однократная публикация (тест)

```bash
cd src
python main.py --mode once
```

Это выполнит один полный цикл: сбор → обработка → публикация.

### 5. Запуск в режиме демона

```bash
cd src
python main.py
```

Бот будет работать постоянно, публикуя посты по расписанию.

## 🎮 Использование

### Режимы работы

```bash
# Демон (постоянная работа)
python main.py

# Однократный запуск
python main.py --mode once

# Вывод статистики
python main.py --mode stats

# С отладочными логами
python main.py --debug
```

### Управление расписанием

Редактируйте в `.env`:

```env
# Количество постов в день
POSTS_PER_DAY=3

# Случайное отклонение (в минутах)
SCHEDULE_JITTER_MINUTES=30
```

## 📊 Мониторинг

### Логи

Логи сохраняются в `logs/neuroscov_bot.log`.

```bash
# Просмотр логов в реальном времени
tail -f logs/neuroscov_bot.log
```

### Статистика

```bash
python main.py --mode stats
```

Покажет:
- Общее количество опубликованных постов
- Посты за последние 7 дней
- Распределение по источникам

## 🔧 Кастомизация

### Промпты для Gemini

Отредактируйте промпты в `.env` или в `src/config_loader.py`:

```python
REWRITE_PROMPT = """
Твой собственный промпт для рерайтинга...
"""

IMAGE_PROMPT = """
Твой промпт для генерации изображений...
"""
```

### Добавление источников

**RSS ленты:**
```env
RSS_FEEDS=https://feed1.com/rss,https://feed2.com/rss
```

**Telegram каналы:**
```env
TELEGRAM_SOURCE_CHANNELS=@channel1,@channel2,@channel3
```

## 🐛 Решение проблем

### Бот не может публиковать в канал

- Убедитесь, что бот добавлен в канал как администратор
- Проверьте правильность `TARGET_CHANNEL_ID`

### Ошибки Telethon

- Удалите файл сессии и пройдите авторизацию заново:
  ```bash
  rm src/neuroscov_user.session
  ```

### Ошибки Gemini API

- Проверьте квоты API: https://ai.google.dev/
- Убедитесь в правильности API ключа

## 📅 Roadmap (Backlog)

- [v1.1] Анализ реакций на посты
- [v1.2] Дообучение на основе реакций
- [v1.3] Интеграция с Яндекс.Дзен
- [v2.0] Генерация Shorts

## 🤝 Вклад в проект

1. Fork репозитория
2. Создайте ветку для фичи (`git checkout -b feature/AmazingFeature`)
3. Commit изменения (`git commit -m 'Add some AmazingFeature'`)
4. Push в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📝 Лицензия

Этот проект лицензирован под MIT License - см. файл [LICENSE](LICENSE).

## 👨‍💻 Автор

**ANISIMOV-STUDIO**

- GitHub: [@ANISIMOV-STUDIO](https://github.com/ANISIMOV-STUDIO)
- Telegram канал: [@scov_touch](https://t.me/scov_touch)

## 🙏 Благодарности

- [Google Gemini](https://ai.google.dev/) - AI обработка текста
- [python-telegram-bot](https://github.com/python-telegram-bot/python-telegram-bot) - Telegram Bot API
- [Telethon](https://github.com/LonamiWebs/Telethon) - Telegram User API
- [APScheduler](https://github.com/agronholm/apscheduler) - Планировщик задач

---

**NeuroScov Bot** - сделано с ❤️ для "Scov Touch"
